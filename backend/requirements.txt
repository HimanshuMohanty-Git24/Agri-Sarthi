# Backend requirements — Web + Voice Gateway
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-dotenv>=1.0.0
httpx>=0.27.0
pydantic>=2.0.0
starlette>=0.45.0

# Conversation logging
requests>=2.31.0

# Lakebase session store (optional)
asyncpg>=0.30.0

# Audio processing (Twilio voice)
audioop-lts>=0.2.1

# Twilio (outbound calls — optional)
twilio>=9.0.0
